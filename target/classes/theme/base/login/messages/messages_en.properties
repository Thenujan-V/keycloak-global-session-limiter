# Global Session Limiter \u2014 login theme message bundle
# Encoding: UTF-8
#
# This file is merged into Keycloak's base login theme message bundle.
# The key is referenced from GlobalSessionLimiter.java via:
#     context.form().setError("sessionLimitReached")
# Keycloak resolves the key at render time against the active locale's bundle.

sessionLimitReached=System full: Maximum concurrent user limit reached.
